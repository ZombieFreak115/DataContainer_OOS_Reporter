# DataContainer_OOS_Reporter

An extension to DataContainer which implements runtime OOS checking of two DataContainer objects.

DataContainer parsing code was taken from the DCON repo (https://github.com/schombert/DataContainer). The license is the same as the license from said repo.